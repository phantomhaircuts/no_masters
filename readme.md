# Decolonize Code;

- no_masters is a simple Z-Shell script to alias and remove the vague and oppressive usage of slave / master commands in the command-line. 
- Download and navigate to the working directory of the zshrc file and run the command:

```
    $ source no_masters.zshrc
```

